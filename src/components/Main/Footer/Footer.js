import React, { Component } from "react";
import styles from "./footer.module.scss";

class Footer extends Component {
  render() {
    return <div className={styles.Footer + " Footer"}>hi</div>;
  }
}

export default Footer;
